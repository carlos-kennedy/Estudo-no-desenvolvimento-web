// childNodes children

const el = document.querySelector("body"); //element

// Os #texts no console.log são os espaços que ficam entre as tags no HTML

// firtsChild firsElementChild
console.log(el.childNodes);
console.log(el.firstChild);

console.log(el.firstElementChild);

// lastChild lastElementChild
console.log(el.lastElementChild);
console.log(el.lastChild);

console.log(el.children);
// No htmlCollection não leva em conta os espaços que no childNodes mostra.
